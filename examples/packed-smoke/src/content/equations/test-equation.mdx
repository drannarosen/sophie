---
id: test-equation
title: "Test Equation (packed-smoke fixture)"
tex: "E = m c^2"
symbols: ["E", "m", "c"]
tags: ["fixture"]
version: "1"
---

import { Observable } from "@sophie/components";

<Observable>
  Placeholder biography children for the packed-smoke `<KeyEquation>` /
  `<EquationRef>` fixture. Not pedagogically meaningful — its only job
  is to register a resolvable refId in the equations collection.
</Observable>
