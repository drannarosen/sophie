---
/**
 * <Sidebar> — left column container; collapsible via
 * `data-sidebar="open"|"closed"` on <html>.
 *
 * PR 1 ships the empty container. PR 3 fills with the
 * module/chapter tree.
 */
---

<nav class='sophie-sidebar' aria-label='Textbook navigation'>
  <slot />
</nav>
